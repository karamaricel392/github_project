def greet_user(name: str) -> None:
    """
    A simple function to greet the user.
    :param name: The name of the user.
    """
    print(f"Hello, {name}!")
    
# Usage example
greet_user("Alice")
